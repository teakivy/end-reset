name: EndReset
version: '${project.version}'
main: me.teakivy.endreset.Main
api-version: 1.18
prefix: ER
authors: [ TeakIvy ]
description: Reset the end with a command
website: https://youtube.com/teakivy
permissions:
  endreset.*:
    description: "All endreset permissions"
    default: op
    children:
      - endreset.resetend
  endreset.resetend:
    description: "Reset the end"
    default: op